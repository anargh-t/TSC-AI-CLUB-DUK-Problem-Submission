// Submit page component - Problem submission form page
// Renders the problem submission form for users to submit AI challenges

import ProblemSubmissionForm from "@/components/forms/problem-submission-form";

const Submit = () => {
  return (
    // Render the problem submission form component
    <ProblemSubmissionForm />
  );
};

export default Submit; 